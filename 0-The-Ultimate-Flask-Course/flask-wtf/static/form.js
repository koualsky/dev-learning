const response = fetch(url, {
    method: 'POST',
    headers: {
        'X-CSRFToken': document.getElementById('csrf_token').value,  // like that i can get csrf_token generated by backend
        'Content-Type': 'application/x-www-form-urlencoded'
    },
    body: // ok so there i can use my form fields generated in backend :)
})